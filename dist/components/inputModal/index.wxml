<block wx:if="{{$taroCompReady}}">
    <view class="{{isShow ? 'device_modal_wrapper_show' : 'device_modal_wrapper_hidden'}}" bindtap="funPrivategYOzw">
        <view class="content" catchtap="detailClcik">
            <view class="title">{{title}}</view>
            <input placeholder="{{placeholder}}" maxlength="{{maxLength}}" class="searchInput" placeholder-style="font-size:30rpx;color:#B9B9B8" bindinput="OnAreaChange" value="{{name}}" />
            <view class="line"></view>
            <view class="bottomView">
                <view class="cancelBtn" bindtap="funPrivatepUHoz">取消</view>
                <view class="confirmBtn" bindtap="funPrivateINdzn">确定</view>
            </view>
        </view>
    </view>
</block>