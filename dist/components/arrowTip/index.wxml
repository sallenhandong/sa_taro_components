<block wx:if="{{$taroCompReady}}">
    <view class="{{isShow ? 'moreListView_wrapper' : 'moreListView_wrapper_hidden'}}">
        <view class="{{'moreListView ' + direction}}">
            <view class="moreListView_item" wx:key="index" wx:for="{{datas}}" wx:for-item="group" wx:for-index="index"><text class="moreListView_item_title">{{group.name}}</text>
                <view class="line"></view>
            </view>
            <view class="arrow"></view>
        </view>
    </view>
</block>